# 작업 우선순위 (수요일 점검 기준)

> 최종 업데이트: 2026-03-13

---

## 🔴 1순위 — 핵심 흐름 완성 (심박→설문→진단)

- [ ] HeartRateCheck → RestTypeTest sessionId 연결 버그 수정
  - HeartRateCheck에서 측정 완료 시 sessionId를 localStorage에 저장
  - RestTypeTest에서 localStorage의 sessionId를 읽어 `/api/diagnosis/calculate`에 전달
  - 사용 후 localStorage에서 sessionId 제거
- [ ] 전체 흐름 실제 테스트 (시뮬레이션 모드)
  - `/heartrate` → 시뮬레이션 측정 시작 → 30초 후 중단 → `/rest-test` → 12문항 → 진단결과 확인
- [ ] DB 데이터 쌓이는지 확인
  - measurement_sessions, diagnosis_results, survey_responses, rest_type_scores

---

## 🔴 2순위 — MainDashboard API 연결

- [ ] 최근 진단 결과 카드 → `/api/diagnosis/latest` 연결
- [ ] 이번 달 휴식 통계 → `/api/stats/monthly` 연결
- [ ] 추천 카드 → `/api/recommendations` 연결
- 선생님이 제일 먼저 보는 화면이라 우선순위 높음

---

## 🟡 3순위 — recommendations 저장 로직 검증

- [ ] RestTypeTest.jsx 122번 줄: 진단 후 자동으로 `/api/recommendations` POST 호출 중 → 실제로 DB에 저장되는지 확인
- [ ] recommendations 테이블에 데이터 쌓이는지 확인
- 이게 없으면 통계/분석 화면이 빈껍데기

---

## 🟡 4순위 — MyPage / 통계 API 연결

- [ ] MyPage.jsx 월간 통계 실데이터 연결
- [ ] 기록 요약, 최근 휴식 목록 API 연결
- [ ] mypage/ 하위 페이지 (MypageHome, MypageStatus 등) 상태 점검

---

## 🟢 5순위 — 나머지 (시간 남으면)

- [ ] MapPage Leaflet + 공공데이터포털 API 연동
- [ ] 장소 Seed 데이터 입력 (공공데이터포털 크롤링)
- [ ] PlaceDetail — 장소 데이터 DB에 있어야 실제로 보임

---

## ✅ 완료된 것

- 로그인 / 회원가입 API 연결
- RestTypeTest (설문 진단) API 연결
- HeartRateCheck (심박 측정) API 연결
- RestRecord (휴식 기록) API 연결
- SignupComplete UI 완성 (API 불필요)
- PasswordReset API 연결
- PlaceDetail API 연결 (장소상세/리뷰/북마크/Leaflet 지도, 길찾기→구글지도)
