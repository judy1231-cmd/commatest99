
# Lotto Number Generator

## Overview

A simple web application to generate random lottery numbers.

## Features

*   Generate 6 unique random numbers between 1 and 45.
*   Display the generated numbers.
*   Keep a history of generated numbers.

## Current Task

### Plan

1.  **`index.html`**:
    *   Change the title to "Lotto Number Generator".
    *   Add a main container for the application.
    *   Add a title heading.
    *   Add a section to display the generated lottery numbers.
    *   Add a button to trigger the number generation.
    *   Add a section for a history of generated numbers.

2.  **`style.css`**:
    *   Add styles for the main container, the number display, the button, and the history section.

3.  **`main.js`**:
    *   Create a function to generate 6 unique random numbers between 1 and 45.
    *   Create a function to display the generated numbers on the screen.
    *   Add an event listener to the button to call the number generation function.
    *   Store the generated numbers in an array to keep a history.
    *   Create a function to display the history.
