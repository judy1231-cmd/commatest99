# 쉼표(,) 테스트 체크리스트

> 테스트 계정: `test@comma.com` / 비밀번호: `Test@1234`
> 관리자 계정: `admin@comma.com` / 비밀번호: `Admin@1234`
> 마지막 업데이트: 2026-03-25

---

## 🔴 최우선 — 핵심 흐름 (진단→추천→기록→개선)

> 멘토링 경고: "핵심 흐름의 데이터 품질을 먼저 완성시켜야 한다"

### 1. 진단 — 설문
- [ ] `/rest-test` 12문항 설문 완료
- [ ] 설문 완료 후 `diagnosis_results` DB 저장 확인
- [ ] `rest_type_scores` 7개 유형 점수 저장 확인
- [ ] 결과 페이지에 주요 휴식 유형 표시 확인

### 2. 진단 — 심박 측정
- [ ] `/heartrate` 접속 → 측정 세션 시작 API 호출
- [ ] `measurement_sessions` DB 저장 확인
- [ ] 심박 데이터 전송 → `heart_rate_measurements` 저장 확인
- [ ] 측정 종료 → `diagnosis_results` 연동 확인

### 3. 추천
- [ ] 진단 완료 후 메인(`/`) 추천 카드 표시 확인
- [ ] 추천 카드 클릭 → `recommendations.click_yn = Y` 저장 확인
- [ ] 추천 카드 저장 버튼 → `recommendations.save_yn = Y` 저장 확인
- [ ] `/map` 추천 장소 마커 표시 확인

### 4. 기록
- [ ] `/rest-record` 휴식 기록 등록
- [ ] `rest_logs` DB 저장 확인 (시작/종료시간, 감정점수 전후, 기분태그)
- [ ] 기록 목록 조회 확인
- [ ] 기록 삭제 (soft delete) 확인

### 5. 개선 — 통계
- [ ] 기록 후 월간 통계 집계 → `monthly_stats` 업데이트 확인
- [ ] `/my` 마이페이지 통계 표시 확인
- [ ] 유형별 비율 JSON 정상 표시 확인

---

## 🟡 인증 흐름

### 회원가입 / 로그인
- [ ] 회원가입 → 쉼표번호 자동 생성 확인
- [ ] 이메일 인증 링크 수신 및 클릭 → 인증 완료
- [ ] 이메일 미인증 상태에서 로그인 차단 확인
- [ ] 로그인 성공 → JWT 토큰 localStorage 저장 확인
- [ ] 로그인 유지 (새로고침 후에도 로그인 상태 유지)

### 비밀번호 찾기
- [ ] `/password-reset` 이메일 입력 → 재설정 링크 수신
- [ ] 재설정 링크 클릭 → 새 비밀번호 설정
- [ ] 새 비밀번호로 로그인 확인

### OAuth2
- [ ] 카카오 로그인
- [ ] 구글 로그인
- [ ] OAuth2 최초 로그인 시 계정 자동 생성 확인

### JWT
- [ ] 비로그인 상태에서 인증 필요 페이지 접근 → 로그인 페이지 리디렉션
- [ ] 리프레시 토큰으로 액세스 토큰 갱신 확인

---

## 🟡 장소 / 지도

- [ ] `/map` Leaflet 지도 로드 확인
- [ ] 장소 마커 표시 (`/api/places` 응답 확인)
- [ ] 마커 클릭 → 장소 팝업 표시
- [ ] `/places/:id` 장소 상세 페이지 진입
- [ ] 장소 상세 — 미니 지도(Leaflet) 표시
- [ ] 북마크 추가 → `place_bookmarks` 저장 확인
- [ ] 북마크 제거 → DB 삭제 확인
- [ ] 리뷰 작성 (별점 + 내용) → `place_reviews` 저장
- [ ] 장소 신고 → `reports` 저장 확인

---

## 🟡 마이페이지

- [ ] `/my` 프로필 정보 (닉네임, 이메일, 쉼표번호) 표시
- [ ] 닉네임 변경 → DB 업데이트 확인
- [ ] 프로필 사진 업로드
- [ ] 배지 목록 표시
- [ ] 배지 자동 지급 확인 (휴식기록 1회 → 첫 번째 배지)
- [ ] 북마크한 장소 목록 표시
- [ ] 내 휴식 기록 목록 표시

### 알림
- [ ] `/notifications` 알림 목록 표시
- [ ] 알림 읽음 처리
- [ ] 헤더 종 아이콘 클릭 → 드롭다운 토글

---

## 🟠 관리자

### 로그인 / 권한
- [ ] `/admin/login` 관리자 계정 로그인
- [ ] 일반 계정으로 `/admin` 접근 시 차단 확인

### 대시보드
- [ ] `/admin` 대시보드 통계 정상 표시

### 사용자 관리
- [ ] `/admin/users` 사용자 목록 조회
- [ ] 사용자 상태 변경 (active → banned) → 알림 자동 발송 확인
- [ ] banned 계정 로그인 차단 확인
- [ ] 금칙어 등록 → 게시글/댓글 작성 시 hidden 처리 확인

### 장소 승인
- [ ] `/admin/places` 대기 중인 장소 목록 표시
- [ ] 장소 승인 → `places.status = approved` 변경 확인
- [ ] 장소 거절 → `places.status = rejected` 변경 확인

### 커뮤니티 관리
- [ ] `/admin/community` 신고 목록 표시
- [ ] 신고된 게시글 숨김/복원 처리
- [ ] `audit_logs` 관리자 액션 기록 확인

### 통계
- [ ] `/admin/analytics` 이벤트 통계 표시
- [ ] analytics_events 데이터 정상 집계 확인

---

## 🟠 커뮤니티 / 챌린지 (2차 MVP)

### 커뮤니티
- [ ] `/community` 게시글 목록 조회
- [ ] 게시글 작성 → `posts` 저장
- [ ] 금칙어 포함 게시글 → `status = hidden` 처리 확인
- [ ] 좋아요 → `post_likes` 저장 / 취소 확인
- [ ] 댓글 작성 → `comments` 저장
- [ ] 게시글/댓글 신고 → `reports` 저장

### 챌린지
- [ ] `/challenge` 챌린지 목록 표시
- [ ] 챌린지 참여 → `challenge_participants` 저장
- [ ] 인증 제출 → `challenge_progress` 저장 (하루 1회 제한 확인)
- [ ] 사진 인증 업로드 → Cloud Storage 저장 확인
- [ ] 달성일수 증가 확인
- [ ] 챌린지 완료 → 배지 지급 확인

---

## 📝 테스트 메모

| 항목 | 결과 | 메모 |
|------|------|------|
| | | |
